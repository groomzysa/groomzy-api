# TO DO UPDATE SUBSCRIPTION TYPES

type Subscription {
  bookingMade(providerId: Int!): bookingMadeSubscription!

  bookingPaid(bookingId: Int!): bookingPaidSubscription!

  bookingCancelled(bookingId: Int!): bookingCancelledSubscription!

  bookingRateService(bookingId: Int!): bookingRateServiceSubscription!

  bookingDone(bookingId: Int!): bookingDoneSubscription!
}
